# Task Spec: Remove two provably dead local bindings in dashboard pages

## Metadata

- Date: 2026-07-16
- Owner: agent-name (maintainer-loop, auto-generated upkeep)
- Status: In Progress — awaiting human review on the PR
- Branch: chore/maintainer-loop/20260716-remove-two-provably-dead-local-bindings-in-dashboard-pages
- Related issue/PR: opened by the maintainer-loop run of 2026-07-16

## Goal

Delete two zero-reference local bindings in the dashboard pages. Both are dead
code with no runtime effect; this is mechanical upkeep, not a behavior change.

## Context Files

- [x] `app/dashboard/page.tsx` (line 354 — `assessedControlsCount`)
- [x] `app/dashboard/compliance-posture/page.tsx` (line 232 — `loadError`)

## Constraints

- Zero behavior change. No control flow, no rendered output, no network calls touched.
- Deletions only — nothing renamed, moved, or refactored.
- Scope stays at exactly these two bindings; no opportunistic cleanup nearby.
- maintainer-loop operates under `push` authority only. Human stays the merge gate.

## Scope

### In scope

- Remove the `assessedControlsCount` declaration in `ComplianceGaps`
  (`app/dashboard/page.tsx`). It is assigned and never read; grep across the
  repo returns a single occurrence — the declaration itself.
- Convert `catch (loadError)` to a bare `catch {` in
  `app/dashboard/compliance-posture/page.tsx`. The binding is never referenced
  inside the catch block, which sets a fixed error string.

### Out of scope

- Any other unused-binding cleanup elsewhere in the repo.
- The surrounding `assessedControlIds` set, which is still live.
- Error-handling behavior in the `catch` block (the fixed message stays as-is).

## Implementation Plan

1. Delete the `const assessedControlsCount = assessedControlIds.size;` line.
2. Replace `catch (loadError) {` with `catch {`.

## Test Plan

- [x] `pnpm typecheck` passes — this is the operative gate. Both bindings are
      local and unexported, so the type checker sees every possible reference
      site; a clean pass proves nothing read them.
- [x] `grep -rn "assessedControlsCount"` returns no hits after the change.
- [x] `grep -rn "loadError"` returns no hits in the touched file after the change.

## Acceptance Criteria

- [x] Both bindings are gone.
- [x] `pnpm typecheck` passes.
- [x] Diff is deletions only, confined to the two named files.
- [ ] Human approved (this PR is auto-generated; review before merge).

## Approval Gate

- [x] Goal is clear
- [x] Context files listed
- [x] Constraints explicit
- [x] Test plan defined
- [x] Acceptance criteria measurable
- [ ] Human approved — NOT approved. Auto-generated by maintainer-loop; Peter
      is the merge gate.
